# FASHIONISTAR AI Engine — ZeroGPU Gradio Space Dependencies
# ─────────────────────────────────────────────────────────────
# PyTorch 2.8.0 is required for ZeroGPU compatibility
# (ZeroGPU supports PyTorch 2.8.0 to latest — see HF ZeroGPU docs)

# Core Gradio + ZeroGPU
gradio>=5.37.0
spaces>=0.37.0

# PyTorch (ZeroGPU provides the CUDA runtime; we pin the version)
torch==2.8.0
torchvision==0.23.0

# Vision AI models
transformers>=4.45.0
accelerate>=0.34.0          # Required for loading models in fp16 / device_map
huggingface-hub>=0.25.0
sentencepiece>=0.2.0

# Body pose estimation
mediapipe>=0.10.14
opencv-python-headless>=4.10.0

# Utilities
numpy>=1.26.0
Pillow>=10.0.0
