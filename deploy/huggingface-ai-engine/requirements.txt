# FASHIONISTAR AI Engine — ZeroGPU Gradio Space Dependencies
# ─────────────────────────────────────────────────────────────
# NOTE: Do NOT pin torch/torchvision here.
# ZeroGPU (zero-a10g hardware) pre-installs the correct PyTorch build.
# Pinning conflicts with HF's build system which injects torch<=2.11.0.

# Core Gradio + ZeroGPU
gradio>=5.37.0
spaces>=0.37.0

# Vision AI models
transformers>=4.45.0
accelerate>=0.34.0          # Required for loading models in fp16 / device_map
huggingface-hub>=0.25.0
sentencepiece>=0.2.0
open_clip_torch>=2.24.0     # For marqo-FashionSigLIP-B-16 (CLIP backend)

# Body pose estimation (Tasks API — replaces deprecated mp.solutions)
mediapipe>=0.10.21          # Tasks API requires 0.10.21+
opencv-python-headless>=4.10.0

# LLM Providers (multi-provider waterfall)
openai>=1.40.0              # SambaNova + Cerebras (OpenAI-compatible)
groq>=0.13.0                # Groq LPU API (Llama 3.3-70B fallback)

# Utilities
numpy>=1.26.0
Pillow>=10.0.0
python-dotenv>=1.0.0        # .env support for local dev
