# FASHIONISTAR AI Engine — ZeroGPU Gradio Space Dependencies
# ─────────────────────────────────────────────────────────────
# NOTE: Do NOT pin torch/torchvision here.
# ZeroGPU (H200 hardware) pre-installs PyTorch. Pinning conflicts with
# HF's build system which injects torch constraints.
# Gradio 6.x is the latest stable — HF Spaces supports it natively.

# Core Gradio + ZeroGPU
gradio>=6.0.0
spaces>=0.37.0

# Vision AI — OpenCLIP (Marqo FashionSigLIP uses OpenCLIP hub format)
open_clip_torch>=3.3.0          # latest: 3.3.0 (Feb 2026)
huggingface-hub>=0.25.0
transformers>=4.45.0            # needed for tokenizer utilities
accelerate>=0.34.0

# Body pose estimation
# MediaPipe 0.10.21+ uses Tasks API
# opencv-python-headless MUST come before mediapipe to prevent libEGL conflict
opencv-python-headless>=4.10.0
mediapipe>=0.10.21

# LLM Providers
openai>=1.40.0                  # SambaNova + Cerebras (OpenAI-compatible API)
groq>=0.13.0                    # Groq LPU (Llama 3.3-70B fallback)

# Utilities
numpy>=1.26.0
Pillow>=10.0.0
python-dotenv>=1.0.0
