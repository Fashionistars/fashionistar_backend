# ===========================================================
# FASHIONISTAR AI — Backend Dependencies
# Django 6.0.2 | Python 3.12+ | Dual-Engine (DRF + Ninja)
# Enterprise-Grade AI Fashion E-Commerce Platform
# ===========================================================


# ═══════════════════════════════════════════════════════════
# 1. CORE FRAMEWORK  (install first — everything depends on these)
# ═══════════════════════════════════════════════════════════
Django==6.0.3

# ── QR Code Generation (device-aware measurement routing) ─────────────────────
qrcode[pil]>=7.4.2              # Server-side QR code PNG generation (brand-coloured, no external API)
Pillow>=10.3.0                  # Image backend for qrcode — also used by Django ImageField
djangorestframework
django-ninja
adrf                            # Async DRF views (used by async_views.py)


# ═══════════════════════════════════════════════════════════
# 2. ENVIRONMENT & CONFIGURATION  (needed before settings load)
# ═══════════════════════════════════════════════════════════
django-environ
environs                        # settings.py: from environs import Env
python-decouple                 # settings.py: from decouple import config


# ═══════════════════════════════════════════════════════════
# 3. DATABASE
# ═══════════════════════════════════════════════════════════
psycopg[binary,pool]                 # PostgreSQL adapter
dj-database-url                 # DATABASE_URL parsing
pgvector==0.3.0                 # Django pgvector adapter



# ═══════════════════════════════════════════════════════════
# 4. AUTHENTICATION & SECURITY
# ═══════════════════════════════════════════════════════════
djangorestframework-simplejwt
PyJWT
cryptography
pycryptodome
django-encrypted-model-fields
django-ratelimit
fido2                           # WebAuthn / biometric auth
google-auth                     # Google OAuth2 token verification
google-auth-oauthlib            # Google OAuth2 flow
google-auth-httplib2            # Google OAuth2 HTTP transport
django-axes                     # Brute-force login protection & IP blocking
django-csp                      # Content Security Policy headers


# ═══════════════════════════════════════════════════════════
# 5. CACHING & REDIS
# ═══════════════════════════════════════════════════════════
django-redis
redis>=5.0                      # Includes redis.asyncio (Phase 3: async hot-path cache)
hiredis                         # C parser for 10x Redis performance


# ═══════════════════════════════════════════════════════════
# 6. BACKGROUND TASKS
# ═══════════════════════════════════════════════════════════
celery
django-celery-beat
flower


# ═══════════════════════════════════════════════════════════
# 7. ASGI / WSGI SERVERS  (Phase 7 upgrade: uvloop + httptools via [standard])
# ═══════════════════════════════════════════════════════════
uvicorn[standard]>=0.29.0       # [standard] activates uvloop + httptools (20-40% latency drop)
daphne
gunicorn>=22.0                  # Gunicorn process manager + UvicornWorker


# ═══════════════════════════════════════════════════════════
# 8. CHANNELS (WebSocket / Real-time)
# ═══════════════════════════════════════════════════════════
channels
channels-redis                  # settings.py CHANNEL_LAYERS backend
django-websockets   
websockets

# ═══════════════════════════════════════════════════════════
# 9. CORS & MIDDLEWARE
# ═══════════════════════════════════════════════════════════
django-cors-headers
whitenoise


# ═══════════════════════════════════════════════════════════
# 10. STORAGE & MEDIA
# ═══════════════════════════════════════════════════════════
django-storages
django-cloudinary-storage
cloudinary
pillow


# ═══════════════════════════════════════════════════════════
# 11. PHONE & SMS
# ═══════════════════════════════════════════════════════════
django-phonenumber-field[phonenumbers]
phonenumbers
twilio
django-phone-verify


# ═══════════════════════════════════════════════════════════
# 12. EMAIL
# ═══════════════════════════════════════════════════════════
django-anymail
django-zoho-zeptomail
email-validator


# ═══════════════════════════════════════════════════════════
# 13. API DOCUMENTATION
# ═══════════════════════════════════════════════════════════
drf-spectacular
drf-yasg


# ═══════════════════════════════════════════════════════════
# 14. ADMIN & AUDIT
# ═══════════════════════════════════════════════════════════
django-jazzmin
django-import-export
dj-control-room[all]


# ═══════════════════════════════════════════════════════════
# 15. LOGGING, MONITORING & OBSERVABILITY  (Enterprise Grade)
# ═══════════════════════════════════════════════════════════
drf-api-logger                  # REST API request/response logging
prometheus_client               # Prometheus metrics endpoint
sentry-sdk[django]              # Error tracking, performance monitoring, tracing
django-silk                     # SQL query profiling & request inspection
django-health-check             # /health/ endpoint for load balancers & uptime
django-structlog                # Structured JSON logging (ELK/Datadog ready)


# ═══════════════════════════════════════════════════════════
# 16. DATA & VALIDATION
# ═══════════════════════════════════════════════════════════
pydantic
django-filter
marshmallow
tablib


# ═══════════════════════════════════════════════════════════
# 17. HTTP & NETWORKING
# ═══════════════════════════════════════════════════════════
requests
httpx
aiohttp
aiohttp-retry
certifi
charset-normalizer
idna
urllib3


# ═══════════════════════════════════════════════════════════
# 18. PAYMENT GATEWAYS
# ═══════════════════════════════════════════════════════════
stripe
rave-python


# ═══════════════════════════════════════════════════════════
# 19. UTILITIES
# ═══════════════════════════════════════════════════════════
asgiref >= 3.8.1
attrs
click
colorama
humanize
jsonschema
packaging
python-dateutil
pytz
PyYAML
setuptools
shortuuid
six
sqlparse
typing_extensions
tzdata
uritemplate
uuid6
wrapt


# ═══════════════════════════════════════════════════════════
# 20. ASYNC INTERNALS  (transitive — pinned for reproducibility)
# ═══════════════════════════════════════════════════════════
aiohappyeyeballs
aiosignal
async-timeout
frozenlist
multidict
propcache
yarl


# ============================================================================================
# 21. TESTING FRAMEWORK
# ============================================================================================
pytest
pytest-django
pytest-asyncio
pytest-cov
factory-boy



# ============================================================================================
# 22. PROVIDER FRAMEWORK 
# ============================================================================================
django-cryptography                     # Required for encryption and decryption of sensitive data
django-model-utils                      # Required for time-based field tracking


# PDF & Fonts
weasyprint
pillow
fonttools
Brotli
tinycss2
cssselect2
html5lib
pyphen
pydyf
webencodings
zopfli

# QR Codes
qrcode

# OpenAI
openai

# =============================================================================
# OBSERVABILITY & STRUCTURED LOGGING  (Phase 5)
# =============================================================================
structlog>=24.0                 # Lazy JSON structured logging (zero-cost when filtered)
django-structlog>=6.0           # Django request context binding for structlog

# =============================================================================
# PROFILING & DEV TOOLING  (N+1 detection, query analysis)
# =============================================================================
nplusone                        # N+1 query detection in test suite
django-debug-toolbar            # SQL query inspector in DEBUG mode (correct package name)


openpyxl>=3.1.0  # Required for xlsx import/export in django-import-export


# =============================================================================
# AI / ML ORCHESTRATION ENGINE  (apps/ai — Phase 6)
# All licenses: Apache 2.0 / MIT — zero cloud API cost
# =============================================================================

# ── AI Orchestration (LangGraph + LangChain stack) ────────────────────────────
langgraph>=0.2.28               # Stateful multi-agent workflow graphs
langchain>=0.3.0                # RAG, tool-use, pipeline building blocks
langchain-core>=0.3.0           # Base abstractions (stable)
langchain-community>=0.3.0      # Community integrations
langchain-ollama>=0.2.0         # Ollama LLM integration node

# ── Local LLM (Ollama — self-hosted, no cloud cost) ───────────────────────────
ollama>=0.3.3                   # Python client for Ollama server
# Model Configuration Details:
#   - Local Dev Model:   llama3.2:3b
#   - Production Model:  llama3.2

# ── Measurement Engine ────────────────────────────────────────────────────────
mediapipe>=0.10.14              # MediaPipe Pose (server-side landmark validation)
opencv-python-headless>=4.9.0   # OpenCV — headless (no GUI) for server safety
numpy>=1.26.4                   # Required by MediaPipe, CLIP, transformers

# ── Recommendation Engine (FashionSigLIP / FashionCLIP) ──────────────────────
open-clip-torch>=2.26.1         # OpenCLIP — loads marqo-FashionSigLIP weights
sentence-transformers>=3.0.0    # Text-only product embeddings + reranking
pgvector>=0.3.0                 # pgvector Django ORM field + HNSW migrations

# ── PyTorch CPU-only (no GPU needed for inference at startup scale) ───────────
# NOTE: Use the CPU-only wheel to keep image size lean.
# Production upgrade: swap to torch with CUDA when GPU instance is provisioned.
# Install separately on server with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0                    # Required by open_clip_torch & sentence-transformers
torchvision>=0.17.0             # Required by open_clip_torch

# ── Background Processing (Celery result/beat already present — extras) ───────
django-celery-results>=2.5.1    # Store Celery results in PostgreSQL for observability

# ============================================================================================
# IN ORDER TO MAKE SURE DJANGO==6.0.3 INSTALLS AND PINNED AFTER BULK REQUIREMENTS.TXT INSTALLATION 
# ============================================================================================

Django==6.0.3